# QR Code Generator

Generate customized QR codes for URLs, text, Wi-Fi configs, or contact cards.

## Arguments
* `input`: The text or URL to encode.
* `box_size`: The size of each box in the QR code (default 10).
* `border`: The border width in boxes (default 4).
