# Regex Tester

Build and test regular expressions interactively.

## Arguments
* `input`: The text block to search.
* `pattern`: The regex pattern to match.
* `flags`: List containing flags e.g., `["i", "m", "s"]`.
